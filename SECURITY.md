# Security policy

Please report security issues privately through GitHub's security advisory
feature rather than a public issue.

MPRIS TUI reads media metadata from the local session D-Bus. It does not fetch
URLs, execute metadata, invoke playback-changing MPRIS methods, or send
telemetry.

